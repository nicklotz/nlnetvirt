name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest
    container:
      image: python:3.12
    steps:
      - name: check
        run: |
          cd 01-refresher-on-osi-model/
          cat server.py
